Compress the caption below to at most {budget} words while keeping the main subject, its action, and the
setting. Drop details in reverse order of importance. Same language and register. Output only the caption.

Caption:
{previous}

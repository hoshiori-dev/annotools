# `annotools.io`

Every source goes through [fsspec](https://filesystem-spec.readthedocs.io), so a local path, an
`s3://` URL and an `https://` URL behave the same. Remote protocols need the matching backend from
`annotools[remote]`.

::: annotools.io
